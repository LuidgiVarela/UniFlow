import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "UniFlow - Revisões",
};

export default function ReviewsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
